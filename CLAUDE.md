# OCR utilities

This project provides wrapper functions for PaddleOCR functions.  The improvement over the basic paddleocr python API is that OCR data pipeline is functionally composable and safety typed using Pydantic models.

