Create a command line tool for ppx.

# Apply OCR and layout parsing to a PDF file.

```
ppx ocr -o ./output ./myfile.pdf --overwrite
```

This will perform OCR analysis and obtain the visual tokens and layout layers.

This will create a directory "./output/myfile" and store the results in it using the `ppx.core.storage.store(...)` function.

